## Packages
framer-motion | Page transitions and animations
jwt-decode | Parsing JWT for client-side token verification

## Notes
- Tailwind Config: Need to add custom font families.
- API requests require JWT token in `Authorization: Bearer <token>` header.
- Wouter handles routing.
- The `api` object from `@shared/routes` provides exact paths and Zod schemas for all endpoints.
